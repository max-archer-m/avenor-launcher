# 迭代 28：升级、回归与版本收尾

> 适用版本：[Avenor Launcher 1.5.0 交付](delivery.zh-CN.md)。状态和实际证据仅归属 `delivery.zh-CN.md`；本契约不授权实施、版本分配、Git、产物、tag、发布或发行操作。

## 目标

形成真实证据，证明所选 `1.5.0` Drawer 旅程能从已接受的前一版本安全升级、保留已接受 Home 状态，并可供作者另行决定版本完成。

## 产品和版本引用

- 产品契约基线：`48d5bd470c84d222b6e89e128f438da1f25e595b`
- [1.5.0 交付](delivery.zh-CN.md)
- [1.4.0 计划交付](../1.4.0/delivery.zh-CN.md)
- [验证指南](../../validation.zh-CN.md)
- [发布治理](../../release.zh-CN.md)
- [产品基础](../../requirements/product-foundation.zh-CN.md)

## 可观察结果

一个可追溯、可安装候选包在作者指定的主设备上升级已接受的前一版本基线，保留可读 Home 模块和无关设置，完成迭代 26–27 的 Drawer 旅程，并如实记录每项结果和缺口，而不暗示已获 tag 或发布授权。

## 纳入工作

- 对齐迭代 26–27 的产品契约、实现、测试、标识和证据。
- 验证原地升级、可读状态保留、缺失 Drawer 字段默认值、全新安装默认值和不可读数据保护。
- 验证 Drawer 搜索、设置行、来源特定应用操作面板、展示设置、模糊降级、持久化、清单变化和导航。
- 回归已接受 Home 模块、收藏、启动、设置、双击锁屏、隐私、离线和资料行为。
- 确认 Drawer 展示设置和收藏模块数据排除在备份之外。
- 生成或识别具有已接受标识、已分配 `versionCode`、源码 commit、签名类别、可用时摘要和升级限制的可安装候选包。
- 记录实际和跳过的检查，并仅在有依据且获授权时更新 `delivery.zh-CN.md`。

## 排除工作

- 在收尾中增加产品功能或改变验收意图。
- 正式发布产物、tag、GitHub Release、上传、发布、商店操作或公开分发。

## 技术变更区域

升级 fixture 和旅程、回归测试、备份检查、构建/产物可追溯性、物理设备验收、证据记录，以及仅限所选范围的聚焦缺陷修正。

## 依赖和顺序

依赖已接受的迭代 26–27 和已接受的前一版本基线。发现的产品或实现缺口回到其归属范围或新的作者决定；收尾不静默吸收它。

## 迁移和兼容性影响

本迭代验证而不重新定义采用。升级必须保留可读 Home 模块、收藏、无关配置和系统授权；缺失的新 Drawer 字段采用当前默认值。不支持降级。

## 安全、隐私、权限和许可影响

未选择新影响。验证最小权限、仅本地搜索和设置、隐私准确性、无障碍隔离和备份排除。任何扩展边界都需要单独审查。

## 风险和未决事项

已接受的前一版本产物、候选源码、已分配 `versionCode`、产物、精确设备证据和 tag 处置在规划时可能尚不存在。仅构建通过不能证明升级或日常使用验收。不得把平台模糊差异误认为已选设置丢失。

## 验收标准

- 迭代 26–27 已有接受结果，且不存在未解决的重大契约不一致。
- 可追溯候选包保持 `com.avenor.launcher`，使用已接受的 `1.5.0` 标识，并记录源码和签名类别。
- 主物理设备从已接受的前一版本升级，并保留可读 Home 模块、收藏、无关配置和系统授权。
- 作者完成所选 Drawer 旅程，以及已接受 Home、设置、启动、双击锁屏、隐私、资料和离线路径，不出现纳入路径崩溃、ANR、破坏性状态错误、错误身份启动或导航死路。
- Drawer 展示设置和收藏模块数据保持排除在 Android 备份和迁移之外。
- 每项失败、跳过检查、未知和限制均准确记录。
- 作者另行决定版本完成和 tag 处置。

## 验证要求

物理设备原地升级、保留/默认采用结果、完整的所选 Drawer 旅程、可追溯候选包身份和作者验收是强制版本证据。全新安装是建议补充证据。聚焦自动化检查以及更广泛 API、OEM、资料、克隆、locale、无障碍、模糊、清单变化、故障注入和性能覆盖保持建议项，除非被提升。实际结果归属 `delivery.zh-CN.md`。

## 相关决定和技术评估

使用适用的持久化、身份、隐私、验证和发布决定。只有收尾证据建立有后果的边界时，才创建或修订持久决定。
