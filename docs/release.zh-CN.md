# 版本、产物与发布治理

> 英文语义源：[release.md](release.md)。
>
> 本文是应用版本、版本归档、APK 产物、签名连续性、Git tag 与 GitHub Release 的权威运营契约。本文不授权任何具体版本、tag、GitHub Release、公开分发或实现范围。

## 项目版本模型

Avenor Launcher 使用项目自定义的 `MAJOR.MINOR.PATCH` 数字版本格式。该格式借用 Semantic Versioning 的三段结构，但以本文而非 Semantic Versioning 官方规范作为约束来源。

- 首个正式应用版本为 `1.0.0`，项目不使用 `0.x.y` 版本。
- 项目不存在预发布版本概念。`alpha`、`beta`、`rc` 以及 SemVer 的预发布或构建元数据后缀均不属于版本格式。
- `1.0.0` 以前的仓库 commit、已 push 源码状态、本地构建及其他工作属于开发证据，不属于正式应用版本。
- 正式版本标识一份可安装且已验证的 Android APK，不因纯文档工作而产生。
- 每个正式版本包含一个或多个已完成迭代。适用的里程碑或版本交付契约从当前产品契约中选择该版本实际交付的子集。
- `1.0.0` 是首个最小可用版本，不要求实现当前产品契约描述的全部行为；其准确交付范围必须通过迭代与里程碑契约确定。

## 数字递进规则

每次发生变化的数字位只能严格加一。除非项目作者明确替换本契约，否则禁止跳号。

- `PATCH` 用于当前产品与版本契约内的兼容性实现修正，每次只加一；`PATCH` 增加时，`MAJOR` 与 `MINOR` 不变。
- `MINOR` 用于当前能力层级内的用户可见能力、主动行为变化或显著可用体验提升，每次只加一；`MINOR` 增加时，`PATCH` 归零。
- `MAJOR` 仅在进入下一个已授权能力层级时加一；`MAJOR` 增加时，`MINOR` 与 `PATCH` 归零。
- 前一位数字增加时，其后的所有数字位始终归零。

`MAJOR` 数字专门对应产品能力层级：

| 版本族 | 能力层级 |
| --- | --- |
| `1.x.x` | V1：固定呈现 |
| `2.x.x` | V2：基础适应 |
| `3.x.x` | V3：AI 能力 |
| `4.x.x` | V4：Agent 能力 |

在进入 V2 的契约得到明确授权前，所有正式版本均保持在 `1.x.x`。重大项目进展或不兼容变更本身不允许增加 `MAJOR`。同一能力层级内的不兼容变更使用下一个 `MINOR` 版本，并必须记录其影响与迁移要求。

纯文档变更永远不改变应用版本。影响 APK 的源码、资源、Manifest、依赖或构建配置变更可以成为新正式版本的理由，但普通代码变更只有在作者宣告版本边界且所需证据齐备后才成为正式版本。

## Android 版本标识

- Android `versionName` 使用本文定义的正式 `MAJOR.MINOR.PATCH` 值。
- Android `versionCode` 在 `1.0.0` 时从 `1` 开始。
- 每次正式 `versionName` 变更，`versionCode` 必须严格加一。
- `versionCode` 不根据 `versionName` 计算，不得跳号、复用或降低。
- 两个不同的正式 `versionName` 不得共用同一个 `versionCode`。
- 不支持降级。

在同一 `MAJOR` 版本族内，任意较旧正式版本必须可以直接升级到任意较新正式版本。跨 MAJOR 升级必须由适用的大版本里程碑契约在发布前定义支持的来源版本、迁移方式、验证和限制。

## 迭代与版本归档

所有纳入迭代完成后，每个正式版本必须由 `docs/archives/v<version>/README.md` 表示。归档包含或链接一个或多个纳入迭代的原始记录，并必须记录：

- `versionName` 与 `versionCode`
- APK 对应的准确 Git commit
- 纳入的迭代及宣告版本边界的理由
- 重要产品变化与实现变化
- 迁移或兼容性影响
- 已完成的验证证据及涉及的设备或环境
- 已知限制、未解决缺陷和其他遗留问题
- APK 文件名与外部存储位置
- APK SHA-256 摘要
- 发布签名证书 SHA-256 指纹
- 存在时的相关 Git tag 与 GitHub Release

归档不得包含私有签名密钥、keystore 密码、密钥密码或其他签名秘密。

## APK 产物契约

每个正式版本经过验证的 APK 存储在本产品仓库之外的 `../max-dev-context` 中。具体目录约定在首个正式版本归档前另行决定。

- APK 文件不得提交到 `avenor-launcher` Git 仓库。
- 本文不决定 APK 是否由 `max-dev-context` 仓库进行 Git 跟踪。在获得明确决定前，该位置只表示外部文件系统存储，不构成提交二进制产物的授权。
- 产品仓库中的记录使用稳定的相对位置或逻辑产物位置，不写入特定机器的绝对路径。
- 记录的 SHA-256 必须由准确的归档 APK 计算，并在复制到外部位置后再次验证。
- 当相关证据可用时，产物记录还必须标识生成并验证 APK 的构建时间与环境。

APK 只有在可安装、通过适用版本验证、匹配归档 commit 与版本标识，并具有已记录摘要与签名指纹后，才属于正式版本产物。

## 签名连续性与保管

从 `1.0.0` 开始的正式版本必须使用同一稳定发布签名身份，使受支持的更新可被 Android 识别为同一应用的更新。

- 发布 keystore 由项目作者创建或明确授权创建，并由项目作者保留所有权和最终控制权。
- 发布 keystore、私钥、密码和签名属性文件必须位于 Git 与权威项目文档之外。
- Agent 仅在作者明确授权后，才可建议或协助生成、构建接入、指纹验证与签名验证。秘密内容和备份位置由作者选择并掌握。
- 每个正式版本归档只记录发布证书的 SHA-256 指纹，永不记录私有签名材料。
- 在 `1.0.0` 之前，项目必须为发布 keystore 和必要恢复信息建立安全存储，以及至少两份相互独立、加密且由作者控制的备份。
- 签名身份丢失、泄露、轮换或迁移到平台托管方案时，必须在宣告下一正式版本前获得作者明确批准、完成影响评估并记录迁移决定。

未来若决定通过 Google Play 或其他商店分发，必须另行定义平台托管签名、上传密钥、渠道要求及迁移影响。目前未授权任何分发平台。

## Git tag 与 GitHub Release

应用版本、Git tag 与 GitHub Release 是相互独立的记录：

- 每个正式版本都必须归档，但并非每个正式版本都创建 Git tag。
- 当积累的相关功能或体验提升形成重要且稳定的实现基线时，适合创建 tag。例如，`1.0.0` 可以不创建 tag，而后续 `1.3.0` 可以成为带 tag 的基线。
- 创建 tag 及其准确目标 commit 必须得到项目作者明确批准。Agent 可以建议某个版本是否值得创建 tag。
- 首个 tag 创建前再决定 tag 的准确命名规则。
- 创建 tag 不要求同时创建 GitHub Release。
- GitHub Release 必须引用一个已存在且获批的 tag，并需要项目作者另行明确批准。
- 项目没有公开分发产品线期间，不要求创建 GitHub Release。未来只有在作者明确批准该分发动作，且满足相应安全与验证门禁时，Release 才可以附带经过验证的 APK。
- 本文与版本归档均不授权创建 tag、远程 push、GitHub Release 或上传 APK。

## 迁移成本约束

产品定义、技术调研、架构与实现必须考虑未来能力层级可能产生的迁移成本。在证据支持时，应保留可替换边界、数据迁移路径以及适当的扩展点或 API 接缝，避免后续能力层级产生不必要的重写。此要求不授权投机性框架、未使用抽象或在产品文档中规定实现细节；所有准备必须与当前证据相称，且不得静默扩大当前范围。

## 尚待实现阶段决定的事项

以下运营细节必须基于实际 Android 项目和发布环境决定，之后才能成为可执行指引：

- `../max-dev-context` 下的准确 APK 目录，以及保留、同步、备份与 Git ignore 规则
- 发布 keystore 格式、参数、安全位置、备份流程及获授权签名流程
- 权威构建、签名、摘要、安装、升级与验证命令
- 准确的 tag 命名约定
- 未来可能采用的分发渠道及其发布门禁

