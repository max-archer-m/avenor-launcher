# Requirements Brief: Product Foundation and Current Scope

> Public semantic source: English. Chinese authoring counterpart: [product-foundation.zh-CN.md](product-foundation.zh-CN.md). Keep both versions materially aligned.
>
## Problem

The author uses an Android phone as their primary device and commonly uses Samsung and Google phones. The Launcher is used whenever the author enters the home screen or needs to find an application or obtain information from it. There is no reliable usage-duration or frequency measurement yet.

The author prefers a custom Home page combined with an application Drawer, but current alternatives do not satisfy all of the following needs:

- Stock launchers and competing products commonly use lists or free-form grids for applications and widgets.
- Highly flexible products allow extensive customization but provide too few constraints to sustain an attractive, comfortable, and minimal result.
- Minimal products may reduce visual noise without matching the author's preferred Home-and-Drawer combination or personal layout needs.

The author therefore wants an Android Launcher with deliberate design constraints that first meets their own long-term daily needs.

## Evidence boundary

### Known facts and author experience

- The author has long-term Android experience and uses Android as their primary phone platform.
- The author has used stock home-screen applications and multiple third-party Launchers as alternatives.
- Current problem evidence consists of the author's sustained usage experience and understanding of their own needs.
- No usage counts, task-duration data, interviews, external user feedback, or market research are currently available.

### Hypotheses to validate

- Limited and intentional layout rules may make an attractive, comfortable, and minimal daily home screen easier to sustain than unrestricted customization.
- Whether Android users other than the author share the same need is unverified.
- Whether the product should later be published and maintained through GitHub or application stores is unverified.

## Project motivation

- Product motivation: create a personally owned home-screen tool suitable for the author's long-term daily use.
- Learning motivation: use a real project to learn agent systems and build experience toward agent-engineering or prompt-engineering work.

The learning motivation may shape the project process and retrospectives, but it must not replace user value, product acceptance, or quality standards.

## Target user and scenarios

### Primary user

The author. Current product trade-offs are evaluated first against whether the author can use the product reliably every day.

Ergonomic decisions primarily optimize for right-hand holding with right-thumb input and left-hand holding with right-hand tapping. Other postures are secondary rather than equal optimization targets.

### Secondary users

There are no identified secondary users. Possible future publication on GitHub or in application stores does not demonstrate broader demand or expand the current target audience.

### Primary scenarios

- Enter the Android default home screen.
- Move from Home to the Drawer, find an installed application, and launch it.
- Open Settings and configure what is necessary to preserve the Launcher's basic utility.
- Continue completing the preceding core tasks without a network connection.

Usage frequency, task duration, and the exact types of information obtained from the home screen have not been measured. The current Settings items are defined in the current product scope.

## Product goal

Provide the author with an Android Launcher suitable for long-term daily use that is fully local and follows least-privilege principles, using Home, Drawer, and Settings to complete the most basic home-screen utility tasks.

The current product scope covers utility, not visual refinement, extensive customization, network information, or mass-market suitability.

The author ultimately wants personalization that fits their own needs and capabilities that some existing alternatives either lack or place behind payment. This is an author expectation and hypothesis, not completed market evidence or a current acceptance criterion.

## Platform and delivery boundary

- Minimum supported version: Android 12 (API 31).
- Current physical validation devices: Samsung Galaxy S23 Ultra on Android 16 and Google Pixel 8 on Android 17.
- Minimum-version validation environment: an Android 12/API 31 emulator unless a representative physical device becomes available.
- Device scope: ordinary Android phones in portrait orientation only.
- Device exclusions: landscape, foldable, tablet, desktop-mode, and external-display adaptation.
- Distribution boundary: maintain the GitHub project for the author's daily use, with no application-store submission, GitHub Release APK, website APK, or other public distribution.
- Store target-API, review, and data-disclosure requirements do not apply to the current distribution boundary; review them if public distribution becomes part of the current scope.

Android 17 corresponds to API 37 according to the [Android 17 SDK setup guide](https://developer.android.com/about/versions/17/setup-sdk). Starting August 31, 2026, Google Play requires new applications and updates to target at least Android 16 (API 36), according to the [Google Play target API requirement](https://developer.android.com/google/play/requirements/target-sdk); this constrains `targetSdk`, not the project's `minSdk`. Because this project does not currently publish to Google Play, its target-API requirement is a future compatibility reference rather than a current release gate. The product compatibility boundary requires `minSdk` 31. For `1.0.0`, the technical assessment must validate `targetSdk` 36 and use the latest stable compatible `compileSdk`; API 37 is the current candidate, with API 36 retained if API 37 still requires preview or release-candidate tooling. The selected values must be implemented and verified in the build configuration when the Android project is created. Reviewed: 2026-08-09.

## Current product scope

### In scope

- Home: one non-pageable, non-collapsible Android home surface displaying system time, date, the favorite-list area, the secondary favorites area, and a Drawer entry. Inside the platform `safeDrawing` bounds, Home applies uniform `8dp` content padding. The page does not scroll vertically. The favorite-list area currently contains at most two equal-status vertical lists that scroll independently only when their content exceeds their shared viewport. The secondary favorites area spans the complete padded Home content width below it with `8dp` vertical spacing, has zero height without content, and contains at most five independently horizontal-scrolling favorite bars.
- Favorites: the author can maintain application entries across at most two vertical favorite lists and the favorite bars according to the Home and application-action-sheet specifications. One vertical list uses the complete padded favorite-list width; two divide that width equally after their gap. Large, medium, and small are selectable persisted list-level display values; every new or migrated list starts at medium.
- Double-tap lock: eligible blank space in Home's basic-information region can request the system lock action after the user explicitly enables Avenor's purpose-limited accessibility service. The service is optional and does not gate independent Launcher paths.
- Drawer: a single-list presentation of every launchable entry successfully read from the sources Android exposes, including cloned application entries when the platform exposes and Avenor successfully reads them. An isolated non-current-profile read failure does not block entries already available from other profiles.
- Settings: current default-Launcher status, an entry to Android system settings for the default home application, and secondary product-information entries. Language follows supported system-locale resources automatically; manual language selection is outside the current scope.
- Complete offline availability for core tasks.
- Local storage of core product data without a self-hosted server, account, or cloud synchronization.
- Only system permissions that are necessary for, and traceable to, core functionality.

### Out of scope

- Visual refinement whose primary goal is aesthetic quality.
- Widgets, folder-like grouping, Home paging, themes, and extensive or unrestricted layout and visual customization.
- One-action clearing or restoration of all local configuration.
- Network-backed information such as weather.
- Accounts, cloud synchronization, or a self-hosted server.
- Behavioral analytics, automatic ordering, recommendations, AI assistance, or agent integration.
- Android Private Space access, presentation, management, favorites, and state restoration, including declaration of `ACCESS_HIDDEN_PROFILES`.
- Business-model validation, formal store release, or mass-market adaptation.

“Out of scope” means excluded from the current product contract, not permanently rejected. Advertising, recommendation feeds, and engagement-maximizing design remain constrained by the long-term boundaries in the project overview.

## Additive requirements

An additive requirement may be added to the current product contract when useful without defining or blocking movement between capability layers. Current candidates include landscape support, foldable and tablet adaptation, themes and colors, weather information, and widgets. Folder-like grouping is excluded because it conflicts with the current direct-access principle.

An additive requirement does not become current scope merely because it can be added independently, and it does not bypass scope, privacy, security, validation, or maintenance review.

## Functional requirements

- The user can launch Avenor Launcher as the Android home-screen entry point.
- Home displays time and date supplied by the device system.
- Home displays locally saved favorite application entries and allows the selected favorite to be launched.
- Home uses content-driven vertical sizing with uniform `8dp` content padding inside the platform `safeDrawing` bounds and `8dp` spacing between adjacent visible modules; Home itself does not scroll vertically. The basic-information module adds no padding of its own. Its time and date rows add `8dp` start and end margins relative to the padded Home content boundary, producing final `16dp` row boundaries from the corresponding safe edges; the time row also adds `8dp` top margin for a final `16dp` top distance. The visible date text adds another `8dp` start inset relative to the time text and therefore begins `24dp` from the safe start edge, while its complete row target remains at the `16dp` boundary. Vertical favorite-list containers, favorite-bar containers, and individual favorite items add no padding beyond Home's uniform content padding. Normal and edit modes preserve the same module and container geometry; editing surfaces, outlines, and controls do not add another outer inset. Basic information wraps its required content and interaction targets. When the natural favorite-list and populated-secondary heights fit the remaining padded content height, both retain natural height and favorite lists remain stationary. When their combined height overflows, the secondary area retains its natural favorite bar height and the favorite-list area receives the remaining viewport above it, where only an overflowing list scrolls vertically. The secondary area spans the complete padded Home content width and has zero height without content.
- Secondary favorites content is user-defined rather than divided by product-defined types. The area contains no more than five `56dp`-high horizontal favorite-bar containers under the current fixed limit; containers and items add no padding. Favorite-bar containers use `8dp` vertical spacing. Each favorite bar contains a finite user-defined application sequence, stays stationary while its entries fit, scrolls horizontally when they overflow, and never wraps cyclically. Each content-measured entry uses a `40dp` application icon with an `8dp` start inset and vertical centering, an `8dp` icon-to-name gap, one static `16sp/24sp` normal-weight application-name line measured up to `64dp`, and an `8dp` trailing inset, producing a maximum item width of `128dp`. The label cap approximates four full-width CJK glyph advances at the active type scale, but every locale uses rendered width rather than literal character count; longer labels use end ellipsis and shorter labels shrink the item. This fixed medium-height presentation has no user size selector. Entries are left-aligned with `8dp` spacing and use the confirmed `6%` light-foreground background, `1dp` boundary at `12%`, `12dp` corner radius, and current light ripple. Normal and edit modes preserve each calculated item width; edit controls may cause earlier ellipsis without expanding the item. Selecting an entry launches its application; long-pressing it opens the existing application action sheet. In edit mode the secondary module receives the shared editing surface, each favorite bar receives an independent transparent `1dp` outline with conditional overflow-edge fades, and each entry exposes the shared trailing drag handle and physical-top-left remove badge. Current favorite bars display no title or title-height reservation; a future title-visibility theme setting is outside this contract.
- Each saved launchable-entry identity has exactly one assignment to one vertical favorite list or one favorite bar and appears at most once across all destinations. Primary, cloned, and work-profile entries remain distinct identities and must not be deduplicated solely by package name. An already-favorited identity is unavailable in edit-mode multi-selection. Existing favorites can move or exchange among vertical lists and favorite bars through the global container-based drag contract.
- The favorite-list area and each vertical list add no padding beyond Home's uniform content padding. The lists form a physical left-to-right horizontal sequence that does not mirror for RTL locales and have an `8dp` inter-list gap. One persisted list takes the complete padded area width; two persisted lists divide that width equally after the gap and share one viewport height while scrolling independently. A persisted list is deleted immediately when it loses its final entry, and remaining widths are recalculated. Large list size uses a `48dp` icon, `64dp` target, and `18sp/28sp` name; medium uses `40dp`, `56dp`, and `16sp/24sp`; small uses `32dp`, `48dp`, and `14sp/20sp`. Size is uniform within a list; every new or migrated list starts at medium, and the edit-mode size control persists a selected large, medium, or small value independently for each list. Favorite items add no padding; their icons use a `16dp` start margin and vertical centering, followed by a `16dp` icon-to-name gap. All sizes use normal weight, system scaling, and one static end-ellipsized name line.
- Drawer action-sheet additions append one application to the current physical-leftmost vertical favorite list by default without navigating to Home or entering edit mode; if none exists, the action creates one list at medium size. Reordering lists changes this default target. Home edit mode additionally provides centered, localized plain-text add actions with a leading text `＋`, no separate icon asset or decorative container, and targets at least `48dp` high and wide enough for their labels. Persisted list ends and existing favorite bars use `＋ Add apps`; the provisional vertical list shown while fewer than two lists exist uses `＋ New favorite list`; and the provisional favorite bar shown while fewer than five favorite bars exist uses `＋ New favorite bar`. Each complete label is one interaction and accessibility target and opens Drawer multi-selection for its captured destination; confirmation appends entries in selection order, and a provisional list or favorite bar becomes persistent only with at least one confirmed entry. A full visible viewport does not reject an addition.
- Drawer multi-selection captures every Home list and favorite bar scroll position. Ordinary cancellation restores each exact position when valid; intervening reliable inventory changes preserve the former topmost visible identity and offset when possible, otherwise clamp to the nearest valid position. Successful confirmation applies the same rule to every non-target and, only when required, minimally scrolls the target on its own axis to make the first still-valid newly appended entry fully visible without forced start alignment, added highlight, Snackbar, or haptic feedback. A new provisional destination begins at its content start; no target scroll occurs when no newly appended identity remains valid.
- Edit mode is entered through a favorite action sheet or a long-press on eligible basic-information blank space. It adds application drag handles, remove badges, add controls, favorite-bar-level fixed controls, and shared editing surfaces. Each application-item remove badge reuses the X artwork as a white X on a solid error-red `20dp × 20dp` background at the item's physical top-left corner. Its interaction target shares those `20dp × 20dp` bounds, composed from the `8dp` edge interval plus half of the `24dp` X-resource extent, and is an author-approved exception to the usual `48dp` minimum. Each application-item drag handle retains a visible `24dp` graphic centered in a `40dp`-wide target that occupies the item's full height, sits flush against its physical right edge, has no end margin, and is a separate author-approved compact-target exception. Favorite-bar-level drag handles retain their existing fixed-rail geometry. Every application, complete-list, and complete-favorite-bar drag starts only from its handle after the platform-standard long-press threshold is recognized. Recognition produces one standard long-press semantic haptic feedback; only subsequent movement creates and moves the preview. Release before recognition does nothing, while movement before recognition cancels the drag candidate and remains ordinary scrolling when the owning viewport can scroll. Home–Drawer navigation and non-edit-mode scrolling are outside this activation rule. Same-container body crossings exchange and persist immediately; same-container gaps never insert. Cross-container occupied bodies and gaps show mutually exclusive exchange or insertion feedback and save once on valid release. Cross-container preview size remains stable until the target presentation applies after release. System Back and the Android system Home action exit edit mode; the Android system Home action also cancels any active Drawer multi-selection and resolves to normal Home.
- Application dragging uses `48dp` edge zones inside the current valid application viewport. After approximately `120ms` stabilization, only that container auto-scrolls on its own axis, with speed increasing toward the edge; leaving the zone, entering invalid space or fixed controls, reaching the boundary, cancellation, or interruption stops it. Complete-list and complete-favorite-bar drags never auto-scroll. An eligible edge fades in a non-interactive light-foreground gradient up to `8%` opacity below continuously recalculated insertion and exchange feedback; an unavailable direction shows no cue, and scrolling itself adds no haptic, arrow, text, ripple, or accessibility node.
- Edit-mode application removal persists immediately and shows a non-blocking long-duration Snackbar with `Removed from favorites` and `UNDO`. Only the latest visible removal can be undone. Undo restores the exact identity, destination, position, list size, and any list or favorite bar deleted because it became empty. Snackbar dismissal, a later removal, edit-mode exit, the Android system Home action, process recreation, or leaving Avenor discards that temporary snapshot without retaining undo history.
- Each editable persisted favorite bar has a fixed `40dp × 56dp` logical-start remove rail, central horizontal application viewport, and fixed `40dp × 56dp` logical-end reorder rail. The start rail is the complete remove target and centers a visible `24dp × 24dp` solid error-red background with a white X, leaving `8dp` on each horizontal side. The end rail matches the width of an application-item drag-handle target and centers its visible `24dp` handle with the same horizontal intervals. Both rails remain outside scrolling content and do not resize the content-measured application items whose widths are capped at `128dp`. One persisted favorite bar hides reorder presentation, interaction, press feedback, and accessibility while preserving its `40dp` end rail; at least two persisted favorite bars show reorder on each. A provisional favorite bar exposes neither favorite-bar-level action. Explicit complete-favorite-bar removal first uses a platform-default `Remove favorite bar?` Dialog with explanatory body, `Cancel`, and destructive `Remove`; Back and outside tap cancel without mutation. Confirmation persists removal and offers latest-only `Favorite bar removed` / `UNDO`. Automatic deletion after the last application leaves a favorite bar does not show this Dialog.
- Complete-favorite bar reorder uses a frozen, non-interactive static-layout preview of the entire favorite-bar container visible at drag start, including both `56dp`-high rails, visible full or partial entries, boundary, and applicable edge fades, while preserving width, `56dp` container height, horizontal scroll state, and grab offset. An equal source placeholder remains. The finger point entering another persisted favorite bar body immediately exchanges and persists order with a short haptic step; gaps never insert, and invalid release or interruption retains the last persisted exchange.
- Every persisted vertical list has a fixed `40dp`-high edit-mode control bar at the top of its existing viewport. The bar has no padding, fill, rounded whole-bar surface, outer border, or following content gap; only a `1dp` light-foreground bottom divider at `12%` opacity separates it from applications. Three adjacent `40dp × 40dp` targets align flush to the physical right edge without RTL mirroring or speculative future slots, ordered physical right-to-left as reorder-list, size, remove-list. A provisional list has no bar, and one persisted list hides reorder while preserving its rightmost `40dp` position. Remove centers a `24dp × 24dp` solid error-red background with a white X; size contains only localized `L/M/S` or `大/中/小` current-value text at its fixed type size; and reorder centers a `24dp` handle. Size and reorder have no persistent individual background and retain bounded pressed state inside their targets; remove adjusts its red state. The controls use no ripple. The bar reduces only the edit-mode application viewport by `40dp` and never changes the favorite area's external height.
- The size action opens a coordinate-anchored dark Material single-selection menu, normally centered `4dp` below its actual button and flipped above or clamped inside the safe content boundary with at least `8dp` clearance when required. The finite menu never scrolls or crops. Its large, medium, and small rows are each `64dp` high and keep the non-mirrored physical order indicator, bundled local Android-default generic icon at `48dp/40dp/32dp`, then localized label. The complete row is one ripple and accessibility target. Selection immediately persists and relayouts the owning list while preserving the previous topmost visible identity and valid offset; outside tap, Back, or reselecting the control closes without change, and persistence failure restores the last saved size.
- Removing a complete vertical list requires the default Android Dialog with `Remove favorite list?`, the explanatory body, `Cancel`, and destructive `Remove`; Back and outside tap cancel without mutation. Confirmation persists removal and offers latest-only `List removed` / `UNDO`, which restores physical position, size, complete membership, and order. Complete-list reorder begins only from its handle, uses a frozen static preview of the visible list viewport plus an equal source placeholder, and immediately exchanges and persists physical positions when the finger enters the other list body. List gaps do not insert.
- Double-tapping eligible blank space in Home's basic-information region requests one system lock action only while the user-authorized Avenor accessibility service is enabled. Time, date-and-weekday, system insets, favorites, and all other interactive targets are excluded; authorization absence or revocation affects only this capability.
- An unreadable or damaged favorite persistence result is presented as a recoverable loading failure rather than an empty favorite list. Avenor preserves the original data, disables favorite mutations, and keeps independent Drawer and application-launch paths available while a read-only Retry is offered.
- The user can access the Drawer from Home.
- Drawer has a fixed transparent `56dp` top app bar. Ordinary mode shows only a left `24dp` Back arrow in a `48dp` target; its middle search slot and right side remain visually empty and non-interactive. Favorite multi-selection instead shows Cancel, a localized target description, and Confirm.
- Favorite multi-selection reconciles live inventory by stable identity without interrupting the mode: new entries appear unselected, selected name or icon changes update in place, and temporary profile or partial-read failures retain entries and selection. Confirmed permanent disappearance silently removes the affected row and closes selection-number gaps. Confirm revalidates and atomically saves only the remaining valid identities in displayed order; an empty valid result returns normally without writing or added feedback. Save failure retains the valid selection and shows the existing update-failure feedback: reliable Content permits Confirm retry or cancellation, while unavailable inventory requires Error-state Retry before Confirm is re-enabled and continues to allow cancellation. An invalid captured destination reference cancels the complete selection and never redirects the addition. It returns with the existing failure feedback only while the originating Home edit context remains active; an ended or replaced context is not recreated. A provisional destination remains a valid creation intent while its originating selection session and Home edit context remain active and the applicable container limit permits creation; absence of a persisted container alone never invalidates it.
- Drawer favorite multi-selection reuses the application inventory, ordering, rows, application anchors, and AlphabetIndex while hiding Settings. Its center action-bar text distinguishes add versus create and vertical list versus favorite bar without persistent container names or ordinals. Every row reserves a fixed `40dp` leading region with a centered `24dp` indicator: available unselected rows use a transparent `1dp` outline circle; selection fills it with the light role, shows a dark `14sp/20sp` one-based order, and applies an `8%` light row background. Already-favorited unavailable rows retain the empty indicator with disabled content opacity and no response. The complete row remains the single toggle-selection interaction and accessibility target and reports unselected, selected ordinal, or already-favorited disabled state. Confirm commits the complete ordered selection; Cancel, Back, downward dismissal, and the Android system Home action discard it unless an atomic save has already started.
- A valid Confirm starts one exclusive atomic save. While it is unresolved, the selection remains visible, Confirm is disabled, and Cancel, row selection, AlphabetIndex, downward dismissal, and Back are inert; no Loading or progress presentation is added. Inventory changes wait for post-save reconciliation. Success transitions once; failure restores interaction, retains current valid selection for retry, and reports the existing failure. The Android system Home action may background Avenor without cancelling the save; its eventual success remains durable, while failure resolves to the last successfully persisted state without a success claim.
- Favorite multi-selection reuses Drawer Loading and Error states while retaining its Cancel and destination context. Loading has no rows, Settings, or AlphabetIndex and disables Confirm. A full Error preserves the in-memory ordered selection, hides unreliable rows, disables Confirm, and allows read-only one-attempt Retry or ordinary cancellation without repeated feedback. Retry success reconciles the latest reliable inventory, selection order, and nearest valid position; Retry failure returns to the same Error. A valid inventory whose entries are all already favorited remains disabled Content with AlphabetIndex, while a valid inventory with no launchable entry uses the existing Error rather than a new Empty state. An inventory failure during an exclusive save waits until that save resolves.
- Whether or not Avenor Launcher is selected as the default home application, the Drawer presents a single list of every application entry successfully read from the visible and launchable sources Android normally exposes within Avenor's current role and least-privilege boundary and allows the selected entry to be launched.
- If a non-current profile cannot be read while another profile produces usable entries, Drawer continues to present and launch the available entries without crashing or replacing the whole surface with Error. The current product does not require a partial-read warning.
- When Android exposes a cloned application entry to the Launcher, the Drawer presents it as an independent launchable entry and does not deduplicate solely by package name.
- Ordinary, work-profile, and cloned launchable entries remain in scope when Android exposes them without hidden-profile access. Private Space entries that require `ACCESS_HIDDEN_PROFILES` are outside the current product scope.
- The Launcher provides English and Simplified Chinese user-visible resources, selects them from the system locale automatically, and falls back to English for unsupported locales.
- The user can open Android system settings for the default home application from Settings.
- The current product does not provide one-action clearing or restoration of all local configuration; users edit configuration through individual settings.
- The core Home, Drawer, and Settings paths do not depend on a network, account, cloud synchronization, or a self-hosted server.
- If a network-backed capability such as weather is added later, its unavailable, unauthorized, or offline state does not block core paths.

## Non-functional requirements

- **Local first:** Current product state and core data remain on the device.
- **Offline capable:** Home, Drawer, application launching, and core Settings remain available without a network connection.
- **Least privilege:** Every permission maps to a current core capability and records its purpose, trigger, denial behavior, and distribution-policy impact.
- **Accessibility-service minimization:** The optional double-tap-lock service is not an accessibility tool, does not request window-content retrieval, does not observe other-application UI for product behavior or analytics, and performs no global action other than the explicit user-triggered system lock request. Device Administrator is not a fallback.
- **Minimum visibility:** The current product requires only discovery and launching of application entries normally exposed to Avenor under its current role and least-privilege boundary; unrestricted access to all installed-package data is outside the current scope. Avenor does not declare `ACCESS_HIDDEN_PROFILES` or expand visibility to Android Private Space. Determine concrete APIs and other necessary manifest declarations or permissions through technical and privacy review.
- **Reliability:** The current product must support the author's ongoing daily use on their actual primary devices; measurable stability thresholds remain unresolved.
- **Compatibility:** Support portrait use on ordinary phones from Android 12/API 31 through Android 17/API 37. Validate the minimum boundary on an API 31 emulator and validate primary daily-use behavior on at least the two recorded physical devices running API 36 and API 37.

## Acceptance criteria

- Given Avenor Launcher is selected as the home application on a supported Android device, when the user performs the system action to return home, then the system displays Avenor Launcher Home.
- Given the user enters Home, when core content finishes loading, then Home displays system time, date, saved favorite application entries, and a Drawer entry.
- Given the user long-presses eligible blank space in the basic-information module, when the long-press is recognized, then Avenor produces long-press feedback and enters Home edit mode without also requesting double-tap lock or opening Drawer.
- Given the secondary favorites area has content, when Home displays it, then it shows no more than five `56dp`-high horizontal favorite-bar containers separated by `8dp`; every container and item has no padding and contains user-defined, content-measured launchable application entries without product-defined type grouping. Each entry uses an `8dp` start inset, `40dp` icon, `8dp` icon-to-name gap, name measured up to `64dp`, and `8dp` trailing inset, for a maximum width of `128dp`; shorter names shrink the item and longer names use end ellipsis. Entries are left-aligned with `8dp` spacing, selecting launches the application, long-pressing opens the existing application action sheet, and each favorite bar's horizontal scrolling stops at its explicit first and last entries without wrapping.
- Given a stable launchable-entry identity is already assigned to any favorite destination, when favorite state is presented, then that identity does not appear again in the same or another list or favorite bar; a primary, cloned, or work-profile entry with a different stable identity remains independently assignable.
- Given the user adds an application from Drawer, including when the leftmost list's visible viewport is already full, then Avenor appends and saves it to the current physical-leftmost vertical favorite list, creating one list at medium size if needed, closes the action sheet, preserves the Drawer position, and does not navigate to Home or enter edit mode.
- Given Home edit mode is active, when the user selects a persisted-list, provisional-list, existing-favorite-bar, or provisional-favorite-bar add control, then Drawer completes its upward transition in multi-selection mode with that exact destination captured.
- Given applications are selected in Drawer multi-selection, when the user confirms, then Avenor appends them once to the captured destination in displayed sequence-number order, returns to the same Home edit mode, preserves every non-target scroll position, minimally reveals the first still-valid new target entry when needed, and creates a provisional list or favorite bar only when at least one entry is confirmed.
- Given one vertical favorite list exists in normal Home, when Home presents the favorite-list area, then that list uses the complete available area width without list or area padding; given two exist, then both divide the width equally after the `8dp` inter-list gap, also without list or area padding.
- Given a vertical favorite list loses its final entry, when the mutation is committed, then Avenor deletes that list and immediately redistributes the favorite-list area across the remaining persisted-list count. Given a favorite bar loses its final application entry, when removal, movement, or inventory reconciliation completes, then Avenor deletes that favorite bar rather than retaining an empty container.
- Given Home edit mode is active, when a favorite is presented, then its item shows a physical-top-left `20dp × 20dp` solid error-red remove control with a white X and the author-approved same-size interaction target, plus a trailing `24dp` drag handle centered in its separate physical-right-edge `40dp`-wide, full-item-height target without an end margin; the two targets do not overlap, and ordinary launch and long-press actions are unavailable until edit mode ends.
- Given the user selects a favorite's remove badge, when the removal is saved, then Avenor removes the entry immediately and shows `Removed from favorites` with `UNDO`; when Undo is selected before dismissal, then the exact entry and any automatically deleted container are restored to their former destination and position.
- Given an Undo Snackbar is visible, when another favorite is removed or the Snackbar is dismissed, edit mode ends, the Android system Home action occurs, the process is recreated, or Avenor is left, then the earlier temporary snapshot is discarded and cannot be restored through an undo history.
- Given Home edit mode shows a persisted vertical list, when its top control bar is presented, then it has only the `1dp` bottom divider and its three adjacent `40dp × 40dp` targets occupy the physical-right edge in right-to-left reorder, size, remove order. Reorder appears only with two persisted lists while preserving its rightmost position when hidden; a provisional list has no bar, and RTL does not mirror these positions.
- Given the user selects a list size control, when the anchored menu opens, then it uses the control's current screen coordinates, avoids safe insets and edges without moving Home, shows three complete-row targets in indicator-icon-label order with actual `48dp/40dp/32dp` bundled default-icon previews, and marks the current value without relying on color alone.
- Given an application drag remains for approximately `120ms` inside an eligible `48dp` edge zone of a scrollable favorite viewport, when more content exists in that direction, then only the current valid container auto-scrolls with increasing edge proximity speed and shows the local light-foreground edge cue below recalculated insertion or exchange feedback; leaving the zone or reaching the boundary stops both scrolling and the cue.
- Given a different list size is selected, when persistence succeeds, then the menu closes, the complete owning list relayouts once without scale animation or additional feedback, its prior topmost visible identity and valid offset are retained, and another list remains unchanged; selecting the current value or dismissing the menu performs no write.
- Given the user activates remove-list, when the platform-default Dialog is open, then Home editing is blocked and Cancel, Back, or outside tap closes it without mutation; when the user confirms `Remove`, then the complete list is persisted as removed and `List removed` / `UNDO` can restore its physical position, size, membership, and order.
- Given the user presses a visible list-level reorder handle, when the platform-standard long-press threshold is not yet recognized, then only its container opacity changes without ripple, scale, flash, animated shadow, preview, or drag haptic; release does nothing and movement cancels the drag candidate. Given the long press is recognized, then exactly one standard long-press semantic haptic feedback arms drag ownership without creating a preview; when movement subsequently begins, then Avenor freezes the current visible list viewport into a non-interactive preview while an equal source placeholder remains.
- Given Drawer multi-selection has unconfirmed entries, when the user selects Cancel, presses Back, or completes a valid downward dismissal, then Avenor discards the complete selection, returns to Home edit mode, and restores every Home container to its exact pre-entry scroll position; when the Android system Home action occurs, it also exits edit mode, displays normal Home, and does not retain the temporary edit viewport.
- Given Drawer multi-selection is active, when reliable inventory confirms a selected identity permanently disappeared, then that row is silently removed and later selection numbers close their gaps; new entries, metadata updates, and temporary profile or partial-read failures do not interrupt the mode or add feedback.
- Given Drawer multi-selection presents an application row, when it is available and unselected, then its fixed leading region shows an empty outlined circle; when the complete row is selected, then the circle fills with its one-based order and the row gains the selected background without adding a second interaction or accessibility target; an already-favorited row remains visibly disabled and does nothing when selected.
- Given a valid Confirm starts saving, when the atomic operation remains unresolved, then duplicate confirmation and every Drawer multi-selection interaction except the Android system Home action are inert without a Loading presentation; when it succeeds, then one downward return occurs; when it fails with reliable inventory, then interaction and the current valid ordered selection are restored for Confirm retry with the existing failure feedback; when inventory remains unavailable, then Error retains that selection and requires inventory Retry before Confirm returns.
- Given favorite multi-selection opens while inventory is Loading or later loses all reliable inventory, when that non-Content state is shown, then Cancel and the destination context remain, Confirm is disabled, rows, Settings, and AlphabetIndex are absent, and Error preserves the in-memory ordered selection for read-only Retry or cancellation without treating identities as permanently absent.
- Given Retry restores reliable inventory, when multi-selection Content returns, then still-valid selections retain order, confirmed absent identities are silently removed and renumbered, temporary unavailability remains selected, and the prior position is restored or clamped to the nearest valid position without success feedback; given all reliable entries are already favorited, then disabled Content and AlphabetIndex remain rather than showing Empty or Error.
- Given the user confirms a revalidated selection, when valid identities remain, then Avenor atomically saves only those identities in displayed order; when persistence fails, then Drawer retains them for retry or cancellation and reports the existing update failure; given the captured destination reference is evaluated, then an active, capacity-valid provisional creation intent remains valid without a persisted container, while a removed persisted destination or an ended, replaced, or no-longer-permitted provisional context cancels rather than redirects the complete selection.
- Given a favorite list's content fits its viewport, when the user drags within that list, then the list does not scroll and an upward drag can enter the Drawer transition; given the content overflows, then the list scrolls first and transfers only same-gesture displacement remaining after its end boundary to the Drawer transition.
- Given the user drags an application within one vertical list or one favorite bar, when the touch point enters another application body, then Avenor exchanges and persists those positions immediately; same-container gaps do not insert, and later invalid release retains the last successfully persisted exchange.
- Given the user drags an application into another vertical list or favorite bar, when the touch point enters an occupied application body, then Avenor shows only exchange feedback; valid release saves both identities' destinations and positions without loss or duplication.
- Given the user drags an application into another vertical list or favorite bar, when the touch point enters a valid first, between-item, last, or provisional-empty boundary, then Avenor shows only an insertion line; valid release saves one insertion, preserves target relative order, applies target presentation, and deletes the source container if it becomes empty.
- Given edit mode contains a persisted favorite bar, when favorite-bar controls are shown, then its equal `40dp × 56dp` outer rails remain fixed, the start rail centers the `24dp × 24dp` solid error-red remove treatment with `8dp` on each horizontal side, and the end rail centers the `24dp` reorder handle with the same intervals. Given only one persisted favorite bar exists, then the end rail remains allocated but exposes no reorder graphic, interaction, press feedback, or accessibility node; given at least two exist, every persisted favorite bar exposes reorder, while a provisional favorite bar exposes neither favorite-bar-level action.
- Given the user activates remove-favorite-bar, when the platform-default Dialog is open, then Home editing is blocked and Cancel, Back, or outside tap closes without mutation; when `Remove` is confirmed, then the complete favorite bar is persisted as removed and latest-only `Favorite bar removed` / `UNDO` can restore its position, membership, and application order.
- Given the user presses a favorite bar's visible fixed reorder handle, when movement begins before long-press recognition, then Avenor cancels the drag candidate, creates no preview, and preserves ordinary horizontal scrolling when available. Given the platform-standard long press is recognized, then Avenor produces exactly one standard long-press semantic haptic feedback and arms drag ownership without creating a preview; when movement subsequently begins, then Avenor freezes the complete currently visible favorite bar window into a non-interactive preview with an equal source placeholder. When the finger enters another persisted favorite bar body, Avenor highlights the target, exchanges and persists the two favorite bar positions immediately, and produces a short movement haptic step, while favorite bar gaps never insert.
- Given two vertical favorite lists exist, when the list-level preview's finger point enters the other list body, then Avenor highlights the complete target, immediately exchanges and persists the physical left-to-right order, produces a short movement haptic step, and changes the default Drawer destination to the new leftmost list; list gaps never insert.
- Given an active drag is interrupted by Back or the Android system Home action, or releases over an invalid Home area, then Avenor removes unsaved preview and feedback and restores the last successfully persisted state without undoing immediate same-container exchanges; the Android system Home action additionally resolves to normal Home.
- Given the accessibility service is enabled, when the user completes a valid double tap entirely within eligible basic-information blank space, then Avenor requests one system lock action; given the service is disabled or revoked, then the gesture issues no action and every independent Launcher path remains available.
- Given favorite persistence cannot be reliably read, when Home presents the favorite region, then it distinguishes the failure from an empty list, preserves the original data, disables favorite writes, offers a read-only Retry, and keeps Drawer discovery and application launching available; when Retry fails, then the same inline error returns without an additional Toast or data overwrite.
- Given the user is on Home, when the user performs the defined Drawer-entry action, then the Drawer presents a single list of application entries successfully read from the sources Android exposes.
- Given Avenor Launcher is not selected as the default home application, when the user launches Avenor Launcher directly and enters the Drawer, then the Drawer still displays every application entry successfully read from the sources that should be visible and launchable under Android platform rules.
- Given a non-current profile read fails while another profile provides usable entries, when Drawer presents the inventory, then the available entries remain visible and launchable without a crash or full-surface Error; entries from the failed profile may be absent and no partial-read warning is required.
- Given Android exposes cloned entries for an application, when the Drawer presents the application list, then every cloned entry appears independently and can be launched.
- Given an entry requires Android Private Space access through `ACCESS_HIDDEN_PROFILES`, when Avenor builds the Drawer inventory, then Avenor does not request that permission, access or display the entry, create a Private Space container, or provide Private Space visibility, lock, unlock, favorite, or restoration behavior.
- Given a launchable application appears in the Drawer, when the user selects it, then the system launches that application.
- Given the system locale is English or Simplified Chinese, when Avenor presents or refreshes its interface, then it uses the corresponding resource set; given another locale, it uses the English fallback.
- Given the user opens Settings, when the user selects the default-home settings entry, then the system opens the corresponding Android settings page.
- Given the device has no network connection, when the user accesses Home, Drawer, application launching, or core Settings, then the core task remains completable.
- Given a non-core network capability is unavailable or unauthorized, when the user performs a core task, then that capability does not block Home, Drawer, application launching, or core Settings.
- Given a system permission is considered for the current product, when requirements and distribution review occur, then the permission is traceable to a necessary core capability and its denial behavior.

## Success assessment and guardrails

### Confirmed direction

- Core product success means that Home, Drawer, and Settings form a minimum utility loop suitable for the author's ongoing daily use.
- Development completion alone is not sufficient evidence of success; results must be observed on the author's real primary devices.
- Success is judged by the author's own daily experience; uncollected external-user opinions are not acceptance evidence.
- Core paths must not crash or become unresponsive because of Avenor Launcher.
- The Drawer must not omit, duplicate, or incorrectly display entries returned by successfully read Android sources. Entries from an isolated failed non-current profile may be absent without blocking available entries.
- Selecting an application must launch the intended application.
- Local configuration must not be unexpectedly lost or corrupted.
- Permission denial may degrade only the dependent capability and must not block other core tasks.
- Home, Drawer, application launching, and core Settings must remain available while the device is offline.

Minimum acceptable performance, power, memory, and startup-response levels will be defined in the quality baseline and are not completion conditions for this product-foundation baseline.

## User control

The current product lets users maintain Home favorites and use individual Settings entries, but it does not provide one-action clearing, export, cloud deletion, or restoration of all configuration. Users can clear application data through Android system settings. Favorite changes must result from explicit user actions. Language presentation follows the system locale rather than adapting from observed behavior.

## Local data boundary

- The only durable user-content data is each launchable application entry saved as a Home favorite together with its vertical-list or secondary-favorite bar destination and user-defined position. Each vertical list has stable membership, order, and one large, medium, or small list-level presentation value; each favorite bar has stable order within the secondary-favorites area and each secondary favorite has one position within its favorite bar. Each stable entry identity has one favorite destination and must not be duplicated across lists or favorite bars. Entry identifiers must distinguish primary, cloned, and work-profile entries exposed by the platform and must not store or deduplicate solely by package name. The latest edit-mode removal snapshot is transient Undo state rather than durable undo history.
- System-locale-derived language presentation and other interface settings in the current product contract are local behavior, not behavioral analytics data.
- Time and date come directly from the device system and are not retained as historical data.
- The current product does not collect or store accessibility window content or events, notifications, contacts, location, clipboard content, files, photos, stable device identifiers, application-usage history, or analytics events.
- The current product has no account, cloud synchronization, server, cloud backup, or cross-device backup. Android cloud backup and device-to-device transfer backup are disabled for Avenor-owned favorite data until a later author-approved restoration contract defines otherwise.

## Dependencies and risks

- The Android home role, application-enumeration approach, cloned-entry visibility, and related manifest declarations or permissions still require technical and privacy review.
- Future support for Android Private Space requires a separate author-approved product capability and renewed interaction, permission, privacy, compatibility, and validation review; implementation discovery does not place it into the current scope.
- Settings includes current default-Launcher status, an entry to system default-home settings, and the product-information entries defined in the Settings interaction specification. Manual application-language selection is outside the current scope.
- Current evidence represents only the author and cannot support a mass-market demand conclusion.
- “Attractive, comfortable, and minimal” has not been converted into observable standards and is not a current acceptance target.
- Using the project to learn agent systems may encourage process or technical complexity beyond product needs; control this through explicit scope changes.
